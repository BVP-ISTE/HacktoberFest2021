def primeNumber(lower,upper):
  for num in range():
   isprime=True
   if num >1:
       for checkprime in range():
           
             break
       if(isprime):
           print (num)
  return
           
primeNumber(30,80)
